---
name: "app-builder"
description: "Construção de aplicações completas com boas práticas."
category: "Development Fundamentals"
source: "notion"
status: "installed"
date_installed: "2026-04-07"
---

# App Builder

## 🎯 Purpose

This skill prevents chaos in large requests by:

## When to Use

Trigger this skill when:

Do NOT use when:

## Implementation

Execute:

```plain text
Identify: app type, target platform, key features, non-goals, constraints.
```

Expected result:

Execute:

```plain text
Pick the simplest stack that satisfies constraints.
If constraints conflict, ask one question.
```

Expected result:

Execute:

```markdown
- [ ] Task: ... → Verify: ...
```

Expected result:

Execute:

```plain text
Assign workstreams (frontend/backend/db/devops/testing).
Run in parallel if independent.
```

Expected result:

Execute:

```plain text
Run checks relevant to touched layers (types, tests, lint, build).
```

Expected result:

## Technology Context

Required Tools:

## Validation

## Related Skills