import { Meta } from "@storybook/blocks";
import { DynamicColorPalette } from "./DynamicColorPalette";
import { TokenUsageTable } from "./TokenUsageTable";
import { ColorExportButton } from "./ColorExportButton";

<Meta title="Design Tokens/Colors" />

# Colors

The foresight design system uses semantic color tokens with **static HSL values** and **dynamic color-mix()** derived colors. Colors adapt automatically when the primary color or theme changes.

---

## Quick Reference

<div className="grid grid-cols-1 md:grid-cols-3 gap-6 my-8">
  <div className="p-5 rounded-lg border border-primary/20 bg-background">
    <h3 className="font-semibold mb-2">1. Semantic Naming</h3>
    <p className="text-sm text-muted-foreground mb-3">Tokens are named by intent, not appearance.</p>
    <div className="space-y-1 text-sm">
      <div>✓ <code className="bg-muted px-1.5 py-0.5 rounded text-xs">bg-destructive</code></div>
      <div>✗ <code className="bg-muted px-1.5 py-0.5 rounded text-xs line-through opacity-60">bg-red-500</code></div>
    </div>
  </div>

  <div className="p-5 rounded-lg border border-primary/20 bg-background">
    <h3 className="font-semibold mb-2">2. Token Pairings</h3>
    <p className="text-sm text-muted-foreground mb-3">Always match backgrounds with their foregrounds.</p>
    <div className="space-y-1 text-xs font-mono">
      <div>bg-primary → text-primary-foreground</div>
      <div>bg-muted → text-muted-foreground</div>
    </div>
  </div>

  <div className="p-5 rounded-lg border border-primary/20 bg-background">
    <h3 className="font-semibold mb-2">3. Token Types</h3>
    <p className="text-sm text-muted-foreground mb-3">Static vs dynamic token behavior.</p>
    <div className="space-y-1 text-sm">
      <div><strong>Static:</strong> Fixed HSL values</div>
      <div><strong>Dynamic:</strong> Derived via color-mix()</div>
    </div>
  </div>
</div>

---

## Visual Palette

Live visualization of current theme colors. Click any swatch to copy the token.

<div className="flex justify-end mb-4">
  <ColorExportButton />
</div>

<DynamicColorPalette showContrast={true} />

---

## Token Reference

Complete list of all tokens with static values, computed values, and WCAG contrast compliance.

<TokenUsageTable />
