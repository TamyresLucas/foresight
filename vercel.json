{
  "version": 2,
  "buildCommand": "test -d packages/design-system && cd packages/design-system; storybook build",
  "installCommand": "npm ci --legacy-peer-deps",
  "outputDirectory": "storybook-static"
}